import { CmsPage } from '@/components/cms/CmsPage';

export const metadata = { title: 'Accessibility Statement' };

export default function AccessibilityPage() {
  return (
    <CmsPage title="Accessibility Statement">
      <p>jobhub.com aims to be usable with keyboard navigation, visible focus, and readable colour contrast. Adjust text size in your browser, and switch language from ENG / বাংলা in the header.</p>
    </CmsPage>
  );
}
