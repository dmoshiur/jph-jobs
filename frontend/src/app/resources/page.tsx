import { CmsPage } from '@/components/cms/CmsPage';

export const metadata = { title: 'Tender / EOI' };

export default function Resources() {
  return (
    <CmsPage title="Tender / EOI">
      <p>Tender and Expression of Interest notices will be listed here — the same utility section you see on bdjobs.com as TENDER/ EOI.</p>
    </CmsPage>
  );
}
